<%= simple_form_for(@foro) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :categoria %>
    <%= f.input :tema %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
